# 夫婦特化型 共通家計簿＆折半精算 PWA 設計書

## 1. プロジェクト概要
* **システム名**: PairPay (ペアペイ)
* **目的**: 夫婦間の「共通費用の立替」「月末の折半/傾斜精算」を最小限のタップ数で記録・同期し、割り勘のストレスをゼロにする。
* **ホスティング**: GitHub Pages (静的ホスティング + PWA)
* **バックエンド / DB**: Supabase (無料枠: 認証・PostgreSQL・リアルタイム同期)
* **対応端末**: スマートフォン (iOS Safari / Android Chrome) PWA対応

---

## 2. システム構成 & アーキテクチャ

```text
[ 夫のスマホ (PWA) ] ──┐
                       ├── HTTPS (Supabase JS SDK) ──> [ Supabase ]
[ 妻のスマホ (PWA) ] ──┘                              ├─ Auth (Email / Magic Link)
                                                      ├─ Database (PostgreSQL)
                                                      └─ Realtime (WebSocket)

[ GitHub Pages ] ──> 静的ファイル配信 (HTML, CSS, JS, manifest.json, sw.js)
```

---

## 3. 主要機能要件

| 機能ID | 機能名 | 詳細仕様 |
| :--- | :--- | :--- |
| **F-01** | **簡易認証・世帯紐付け** | 夫婦共通の「世帯ID（household_id）」を持ち、2人の端末から同一世帯のデータにアクセスする。 |
| **F-02** | **親指1タップ支出記録** | ・金額（テンキー入力）<br>・カテゴリ（食費・日用品・光熱費・外食・特別費・その他）<br>・立替者トグル（夫 / 妻）<br>・日付（デフォルトは本日） |
| **F-03** | **リアルタイム折半/傾斜精算** | ・当月の「夫立替合計」「妻立替合計」「総支出」を集計。<br>・負担比率（50:50, 60:40 など）に基づき、「妻 → 夫 に ¥XX,XXX 送金で完了」を常時1行表示。 |
| **F-04** | **精算アクション** | 精算額をタップすると「PayPay送金リンクを開く」または「LINE精算テキストをコピー」。精算完了ボタンで当月分をアーカイブ。 |
| **F-05** | **カテゴリ別内訳** | 当月の支出比率をシンプルなプログレスバーまたは円グラフで可視化。 |
| **F-06** | **PWA & オフライン** | ホーム画面追加対応、アプリアドレスバー非表示、通信復帰時の自動再同期。 |

---

## 4. データベース設計 (Supabase / PostgreSQL)

### 4.1 テーブル定義

```sql
-- 1. 世帯テーブル (夫婦で1レコードを共有)
create table households (
  id uuid primary key default gen_random_uuid(),
  name text not null default '我が家',
  ratio_user1 integer not null default 50, -- 夫の負担割合 (%)
  ratio_user2 integer not null default 50, -- 妻の負担割合 (%)
  created_at timestamp with time zone default now()
);

-- 2. プロファイルテーブル (Supabase Authと連携)
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  household_id uuid references households(id) on delete cascade,
  display_name text not null, -- '夫' または '妻'
  created_at timestamp with time zone default now()
);

-- 3. 支出レコードテーブル
create table expenses (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households(id) on delete cascade,
  title text, -- メモ（任意、例: スーパー、ニトリ）
  amount integer not null check (amount > 0),
  category text not null check (category in ('food', 'daily', 'utility', 'dining', 'special', 'other')),
  paid_by_name text not null, -- '夫' または '妻'
  expense_date date not null default current_date,
  is_settled boolean not null default false, -- 精算済みフラグ
  created_at timestamp with time zone default now()
);

-- インデックス
create index idx_expenses_household_date on expenses(household_id, expense_date);
create index idx_expenses_settled on expenses(household_id, is_settled);
```

### 4.2 行レベルセキュリティ (RLS) ポリシー

```sql
alter table households enable row level security;
alter table profiles enable row level security;
alter table expenses enable row level security;

-- 同一世帯のユーザーのみ支出データの読み書きを許可
create policy "Users can view expenses in same household"
on expenses for select
using (
  household_id in (
    select household_id from profiles where id = auth.uid()
  )
);

create policy "Users can insert expenses in same household"
on expenses for insert
with check (
  household_id in (
    select household_id from profiles where id = auth.uid()
  )
);

create policy "Users can update/delete expenses in same household"
on expenses for all
using (
  household_id in (
    select household_id from profiles where id = auth.uid()
  )
);
```

---

## 5. 精算計算ロジック

### 5.1 変数定義
* $T_{\text{total}}$: 当月の未精算支出合計
* $P_{\text{husband}}$: 夫が立替えた合計金額
* $P_{\text{wife}}$: 妻が立替えた合計金額
* $R_{\text{husband}}$: 夫の負担比率（例: 0.5）
* $R_{\text{wife}}$: 妻の負担比率（例: 0.5）

### 5.2 算出式
$$夫の負担すべき額 = T_{\text{total}} \times R_{\text{husband}}$$
$$妻の負担すべき額 = T_{\text{total}} \times R_{\text{wife}}$$
$$\Delta_{\text{husband}} = P_{\text{husband}} - 夫の負担すべき額$$

* $\Delta_{\text{husband}} > 0$ の場合: **妻から夫へ $\Delta_{\text{husband}}$ 円 送金**
* $\Delta_{\text{husband}} < 0$ の場合: **夫から妻へ $\vert{}\Delta_{\text{husband}}\vert{}$ 円 送金**
* $\Delta_{\text{husband}} = 0$ の場合: **精算不要（差額0円）**

---

## 6. 画面UI・ワイヤーフレーム (モバイル縦画面)

```text
┌──────────────────────────────────────┐
│  PairPay                 2026年9月   ⚙ │
├──────────────────────────────────────┤
│  【今月の精算ステータス】                    │
│  夫の立替: ¥62,000   妻の立替: ¥38,000│
│  総支出: ¥100,000 (折半 50:50)        │
│  ─────────────────────────────────── │
│  👉 妻 ➔ 夫 へ 【¥12,000】 送金      │
│  [ LINEに請求文をコピー ] [ 精算完了 ] │
├──────────────────────────────────────┤
│  【カテゴリ別内訳】                      │
│  食費: 45% | 日用品: 20% | 外食: 35%   │
│  [■■■■■■■■■■■■□□□□□□□□□□□□□□]     │
├──────────────────────────────────────┤
│  【最近の支出履歴】             [すべて表示] │
│  ・09/18 スーパー     ¥3,240  [夫] 🗑 │
│  ・09/17 薬局         ¥1,890  [妻] 🗑 │
│  ・09/16 外食         ¥6,500  [夫] 🗑 │
├──────────────────────────────────────┤
│  【 ＋ 支出を記録する（ボトムシート開閉）】 │
└──────────────────────────────────────┘

─── [ 支出入力ボトムシート (モーダル) ] ───
  金額: [ ¥ 3,240       ]
  誰が払った？: [ (夫) ]  [ 妻 ]
  カテゴリ: [ 食費 ] [ 日用品 ] [ 外食 ] [ その他 ]
  メモ（任意）: [ スーパー                  ]
  [ この内容で記録する (大ボタン) ]
────────────────────────────────────────
```

---

## 7. 技術スタック & ファイル構成

### 7.1 技術選定
* **ビルドツール**: Vite + TypeScript
* **CSS**: Tailwind CSS (モバイルファースト設計)
* **クライアントライブラリ**: `@supabase/supabase-js`
* **アイコン**: Lucide Icons
* **PWA**: `vite-plugin-pwa`

### 7.2 リポジトリ構成案
```text
pair-pay/
├── index.html
├── manifest.json
├── package.json
├── vite.config.ts
├── src/
│   ├── main.ts               # エントリーポイント & ルーティング
│   ├── supabase.ts           # Supabase クライアント初期化
│   ├── types.ts              # データ型定義 (Expense, Household, Profile)
│   ├── logic/
│   │   └── settlement.ts     # 折半・傾斜精算の計算純粋関数
│   ├── components/
│   │   ├── SettlementCard.ts # 精算サマリー表示コンポーネント
│   │   ├── ExpenseList.ts    # 支出リスト & 削除機能
│   │   ├── ExpenseModal.ts   # 金額・カテゴリ入力モーダル
│   │   └── CategoryBar.ts    # カテゴリ比率バー
│   └── styles/
│       └── main.css
└── public/
    ├── icon-192.png
    ├── icon-512.png
    └── sw.js
```

---

## 8. Antigravity向け 実装指示プロンプト

```text
# 実装プロンプト: PairPay (夫婦特化型家計簿PWA)

## 目的
添付の設計書に従い、GitHub Pagesでホスティング可能な夫婦向け家計簿PWAを実装してください。

## 要件
1. Vite + TypeScript + Tailwind CSS を使用してください。
2. Supabase SDK (@supabase/supabase-js) を組み込み、設計書のテーブル構造（households, profiles, expenses）と連携させてください。
   - 環境変数 VITE_SUPABASE_URL と VITE_SUPABASE_ANON_KEY を使用すること。
3. モバイルファーストで構築し、画面下部の押しやすい位置に入力トリガーを配置してください。
4. 金額・支払者（夫/妻）・カテゴリを素早く入力できるモーダル/ボトムシートを実装してください。
5. settlement.ts に設計書記載の折半精算ロジックを実装し、リアルタイムに「どちらからどちらへ何円送金するか」を反映させてください。
6. LINE共有テキスト生成機能（クリップボードAPI）を実装してください。
7. PWA対応のため、Service Workerおよびマニフェストファイルを含めてビルドできるように設定してください。
```